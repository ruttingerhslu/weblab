import { Box, Typography } from "@mui/material";

const Home = () => {
  return (
    <Box sx={{ display: "flex" }}>
      <Typography variant="h4">Welcome to Technology Radar</Typography>
    </Box>
  );
};

export default Home;
